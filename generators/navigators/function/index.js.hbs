import React, { FC } from 'react';
import {
  View,
  Text,
} from 'react-native';

import {t} from '../../../../i18n';
import styles from './styles';

export interface {{pascalCase screen}}Props { }

const {{pascalCase screen}}: FC<{{pascalCase screen}}Props> = () => {
  return (
    <View style={styles.container}>
      <Text>{t('{{lowerCase name}}:{{lowerCase name}}')}</Text>
    </View>
  );
};

export default {{pascalCase screen}};